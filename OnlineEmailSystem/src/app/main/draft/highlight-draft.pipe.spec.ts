import { HighlightDraftPipe } from './highlight-draft.pipe';

describe('HighlightDraftPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightDraftPipe();
    expect(pipe).toBeTruthy();
  });
});
