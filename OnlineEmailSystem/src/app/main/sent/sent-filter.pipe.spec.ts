import { SentFilterPipe } from './sent-filter.pipe';

describe('SentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
