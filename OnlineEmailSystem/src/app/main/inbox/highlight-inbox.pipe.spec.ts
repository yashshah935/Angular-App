import { HighlightInboxPipe } from './highlight-inbox.pipe';

describe('HighlightInboxPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightInboxPipe();
    expect(pipe).toBeTruthy();
  });
});
